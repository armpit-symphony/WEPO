# Legacy Root Test Harnesses

This directory contains older root-level ad hoc test harnesses that were left
beside the active WEPO platform code.

They are historical debugging and verification scripts, not the canonical WEPO
acceptance path.

Use the current local verification flow instead:

```bash
/home/sparky/WEPO/wepo-blockchain/scripts/run_canonical_fee_smoke.sh
```
