# Legacy Backend Backups

This directory contains unreferenced backend backup files that were left beside
the active backend implementation.

They are not part of the canonical WEPO backend path.

Use the active backend and canonical local verification flow instead:

```bash
/home/sparky/WEPO/wepo-blockchain/scripts/run_canonical_fee_smoke.sh
```
